"Test that the mapper-cryptography plugin is loaded in Elasticsearch":

  # Use the Cat Plugins API to retrieve the list of plugins
  - do:
      cat.plugins:
        local: true
        h: component

  - match:
      $body: /^mapper-cryptography\n$/
